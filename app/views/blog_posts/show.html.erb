<p id="notice"><%= notice %></p>

<div class="panel panel-primary">
	<div class="panel-heading">
		<p><%= @blog_post.title %></p>
	</div>
	<div class="panel-body">
		<p>by <strong><%= link_to @blog_post.user.username, user_posts_path(id: @blog_post.user.id) %></strong> 
		<p>submitted <%= @blog_post.created_at.strftime('%m/%d/%Y at %I:%M%p') %></p>
		<p><%= @blog_post.blog_entry %></p>
	</div>
	<% if current_user.id == @blog_post.user.id %>
	<div class="panel-footer">
	  <%= link_to 'Edit', edit_blog_post_path(@blog_post), class: "btn btn-warning" %>
	  <%= link_to 'Destroy', @blog_post, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-danger" %>
	</div>
<% end %>
</div>


<%= link_to 'Edit', edit_blog_post_path(@blog_post) %> |
<%= link_to 'Back', blog_posts_path %>
<br>
<br>
<div class="panel panel-success">
		<div class="panel-heading">
			Make a Comment
		</div>
		<div class"panel-body">
			<%= render 'comments/form' %>
		</div>
		<br>
</div>


<div class="panel panel-default">
	<div class="panel-header">
		<h4>Comments</h4>
	</div>
	<% @blog_post.comments.each do |comment| %>
	<div class="panel-body">
		<p><strong><%= link_to comment.user.username, user_posts_path(id: @blog_post.user.id) %></strong> said:</p> 
		<p><%= comment.comment_entry %></p>
		<p>submitted <%= comment.created_at.strftime('%m/%d/%Y at %I:%M%p') %></p>
	</div>
	<% if current_user.id == @blog_post.user.id %>
	<div class="panel-footer">
	  <%= link_to 'Edit', edit_blog_post_path(@blog_post), class: "btn btn-warning" %>
	  <%= link_to 'Destroy', @blog_post, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-danger" %>
	</div>
<% end %>
	<% end %>
</div>


<%= link_to 'Back', blog_posts_path %>

<br>
<br>









